// The receipt-operations pages have an explicit, exhaustive sentence ledger.
// This is intentionally data, so the test can reject a new behavioral sentence
// that has neither executable evidence nor an honest declared gap.
export const claims = [
  { file: "docs/reference/receipt-operations.md", status: "DECLARED", text: "Seal is the canonical reference for the four operations of the shipped v2 receipt verifier, `checker/seal-receipt-v2.mjs`.", reason: "Canonical ownership is a documentation relationship, not a runtime property that this repository can derive from the verifier." },
  { file: "docs/reference/receipt-operations.md", status: "CHECKED", text: "It describes TESTED behaviour of that checker, not a claim that a receipt makes an outside event true.", reason: "The coverage test executes the shipped checker and separately asserts the explicit occurrence ceiling; no outside event is treated as evidence." },
  { file: "docs/reference/receipt-operations.md", status: "CHECKED", text: "READ parses and displays the receipt.", reason: "The parsed vector reaches verify() and the rendered output is compared with format()." },
  { file: "docs/reference/receipt-operations.md", status: "CHECKED", text: "It makes no authenticity claim, executes nothing, and asserts no validity.", reason: "The unsigned vector produces signature=false while read/validate/replay are independently reported; the verifier has no execution step." },
  { file: "docs/reference/receipt-operations.md", status: "CHECKED", text: "VALIDATE performs non-executing integrity checks: syntax, duplicate-member refusal, schema, canonical-value rules, field commitments, and a signature against the SUPPLIED key.", reason: "The verifier's read/validate path and supplied-key signature path are exercised by the receipt test and source-backed tamper assertions." },
  { file: "docs/reference/receipt-operations.md", status: "CHECKED", text: "It establishes that the receipt has the canonical parsed value signed by that key, not that the key is trusted.", reason: "The unsigned parsed vector has signature=false and authority NOT ESTABLISHED; the output assertion binds those returned fields." },
  { file: "docs/reference/receipt-operations.md", status: "CHECKED", text: "REPLAY loads the receipt's recorded inputs, recomputes with the verifier's local kernel, and compares its verdict with the recorded verdict.", reason: "verify() passes the receipt inputs to the verifier-local decision runner and refuses when its verdict differs from the recorded verdict." },
  { file: "docs/reference/receipt-operations.md", status: "CHECKED", text: "It establishes that those verdicts agree under those inputs, not that the verifier used the original kernel, nor authority or occurrence.", reason: "The vector returns replay=true from the local runner comparison, the receipt carries no original-kernel identity, and the same result reports authority and occurrence as NOT ESTABLISHED." },
  { file: "docs/reference/receipt-operations.md", status: "CHECKED", text: "VERIFY applies a NAMED profile to the available evidence and trust inputs, then emits a row-by-row report.", reason: "The verifier returns the named evidence rows and the refusal matrix exercises trust-input presence." },
  { file: "docs/reference/receipt-operations.md", status: "CHECKED", text: "It is never a synonym for “everything here is true”.", reason: "The shipped verifier returns verify=false while read, validate, and replay are true." },
  { file: "docs/reference/receipt-operations.md", status: "CHECKED", text: "No receipt-only profile may claim that the downstream effect occurred.", reason: "The occurrence row remains NOT ESTABLISHED and no receipt-only option can change it." },
  { file: "docs/reference/receipt-operations.md", status: "CHECKED", text: "The verifier currently returns `verify: false` for every receipt.", reason: "The parsed vector's verify result is asserted false, and the verifier's returned field is the source of the assertion." },
  { file: "docs/reference/receipt-operations.md", status: "CHECKED", text: "A positive VERIFY is therefore unreachable.", reason: "The same runtime result is false and the verifier has no positive VERIFY path in Phase A." },
  { file: "docs/reference/receipt-operations.md", status: "CHECKED", text: "Supplying `authorityRoot` refuses with `authority roots cannot be checked by the v2 verifier`; supplying `occurrenceWitness` refuses with `occurrence witnesses cannot be checked by the v2 verifier`.", reason: "Both option names are passed to verify() with each documented present value and the exact refusal messages are asserted." },
  { file: "docs/reference/receipt-operations.md", status: "CHECKED", text: "The refusal is honest: the Phase A verifier has no format or check for either input, so accepting either would turn an unchecked assertion into supposed evidence.", reason: "The two options are rejected before any result is returned, so neither can become evidence in the shipped verifier." },
  { file: "docs/reference/receipt-operations.md", status: "CHECKED", text: "Presence is tested, including empty, null, zero, and false values.", reason: "The test invokes both options with empty string, null, zero, and false and requires the documented refusal every time." },
  { file: "docs/reference/receipt-operations.md", status: "CHECKED", text: "The following block is rendered from the versioned [v1 vector set](receipt-operations-v1/README.md) by the shipped verifier.", reason: "The fence test reads the versioned vector, calls verify(), and compares the block byte-for-byte with format()." },
  { file: "docs/reference/receipt-operations.md", status: "CHECKED", text: "It is checked in CI against `format()`.", reason: "The receipt-operations test is in the repository test roster and performs the exact format comparison." },
  { file: "docs/reference/receipt-operations.md", status: "CHECKED", text: "The five rows are independent: structure, signature and bindings, Verifier-local verdict, authority key, and event occurrence.", reason: "The format output is generated from five separate result fields and the test asserts the returned fields independently." },
  { file: "docs/reference/receipt-operations.md", status: "CHECKED", text: "In particular, `REPLAY` does not establish authority, and no row establishes occurrence.", reason: "The replayed vector returns authority and occurrence as NOT ESTABLISHED, which is asserted against the rendered result." },
  { file: "docs/reference/receipt-operations.md", status: "DECLARED", text: "The normative envelope and canonicalisation rules remain in [SEAL-RECEIPT-V2.md](../SEAL-RECEIPT-V2.md).", reason: "Document location and normative ownership are repository editorial facts; this test does not duplicate or interpret the separate specification." },
  { file: "docs/reference/receipt-operations.md", status: "DECLARED", text: "This page owns the meaning of the four operational verbs and the trust ceiling of the shipped verifier.", reason: "A page's editorial scope cannot be established from verifier execution; it is an intentional documentation boundary." },
  { file: "docs/reference/receipt-operations-v1/README.md", status: "CHECKED", text: "This versioned vector is an unsigned `seal.receipt/v2` receipt whose recorded kernel decision is `BLOCK`.", reason: "The test parses the versioned JSON, asserts no signature, asserts the v2 envelope, and compares the recorded verdict with the replayed result." },
  { file: "docs/reference/receipt-operations-v1/README.md", status: "CHECKED", text: "The vector covers READ, VALIDATE, REPLAY, the unsigned-signature boundary, and the currently unreachable VERIFY result.", reason: "The test executes the vector and asserts read=true, validate=true, replay=true, signature=false, and verify=false." },
  { file: "docs/reference/receipt-operations-v1/README.md", status: "DECLARED", text: "It does not cover a valid signature, authority-root or occurrence-witness formats, producer output, or proof that a downstream event occurred.", reason: "Absence of all future formats, producer output, and real-world occurrence cannot be exhaustively established by this single-vector runtime test; the page records the explicit scope limitation." },
];
